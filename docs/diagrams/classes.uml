classDiagram
    direction RL

    User "1" o-- "*" Reservation : réserve

    class User {
        - id : int
        - email : string
        - password: string
        - role : string
        - firstName : string
        - lastName : string
        - label : string 

        + getAllUsers()
        + getUserById()
        + searchUser()
        + register()
        + editUser()
        + deleteUser()
        + login()
        + logout()
    }

    class Reservation {
        - id: int
        - number : int
        - status : string
        - paid_at : Date
        - total : double
        - mode : string

        + getAllReservations()
        + getReservationById()
        + searchReservation()
        + addReservation()
        + editReservation()
        + deleteReservation()
    }