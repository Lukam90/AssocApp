erDiagram
    User {
        id int
        email varchar
        password varchar
        first_name varchar
        last_name varchar
        phone varchar
        label varchar
        is_active boolean
        is_member boolean
        is_notified boolean
        is_optin boolean
        role label
    }

    Notification {
        id int
        content text
        send_at date
    }

    Newsletter {
        id int
        object varchar
        content text
        is_published boolean
        send_at date
    }

    Ticket {
        id int
        full_name varchar
        label varchar
        email varchar
        phone varchar
        type varchar
        object varchar
        content text
        created_at date
        status varchar
    }

    Mode {
        id int
        label varchar
    }

    Event {
        id int
        planned_at date
        title varchar
        num_available int
        fixed_price double
    }

    Document {
        id int
        title varchar
        file blob
    }

    Reservation {
        id int
        number int
        status label
        paid_at date
        total double
        comment text
    }

    User o{ -- || Notification : "reçoit"
    User o{ -- }| Newsletter : "envoie"
    User o{ -- o| Ticket : "crée"
    User || -- }o Mode : "paie avec"
    User o{ -- }o Event : "s'inscrit à"

    Mode o{ -- || Reservation : "règle"
    Event o{ -- || Reservation : "concerne"

    Document o| -- }o Event : "concerne"