erDiagram
    User {
        id int
        email varchar
        password varchar
        first_name varchar
        last_name varchar
        phone varchar
        label varchar
        is_member boolean
        is_optin boolean
        role label
    }

    Message {
        id int
        object varchar
        content text
        is_newsletter boolean
    }

    Mode {
        id int
        label varchar
    }

    Event {
        id int
        planned_at date
        title varchar
        num_available int
        fixed_price double
    }

    Document {
        id int
        title varchar
        file blob
    }

    Reservation {
        id int
        number int
        status label
        paid_at date
        total double
        comment text
    }

    User o{ -- }| Message : "envoie"
    User o{ -- }| Message : "reçoit"
    User || -- }o Mode : "paie avec"
    User o{ -- }o Event : "s'inscrit à"

    Mode o{ -- || Reservation : "règle"
    Event o{ -- || Reservation : "concerne"

    Document o| -- }o Event : "concerne"