erDiagram
    User {
        id int
        email varchar
        password varchar
        first_name varchar
        last_name varchar
        phone varchar
        label varchar
        is_member boolean
    }

    Role {
        id int
        label varchar
    }

    Membership {
        id int
        price double
        year int
    }

    Message {
        id int
        object varchar
        context text
    }

    Mode {
        id int
        label varchar
    }

    Event {
        id int
        planned_at date
        label varchar
        available int
        price double
    }

    Document {
        id int
        title varchar
        file blob
    }

    Reservation {
        id int
        paid boolean
        paid_at date
        total double
        comment varchar
    }

    Status {
        id int
        label varchar
    }

    Table {
        id int
        price double
        pos_x int
        pos_y int
    }

    User || -- }o Role : "exerce"
    User o{ -- || Message : "envoie"
    User o{ -- || Message : "reçoit"
    User o{ -- || Document : "télécharge"
    User || -- }o Mode : "paie avec"
    User o{ -- }| Event : "s'inscrit à"
    User |{ -- || Table : "réserve"
    User o{ -- || Membership : "paie"

    Mode o{ -- || Reservation : "règle"
    Mode o{ -- || Membership : "règle"

    Table || -- }| Reservation : "regroupe"
    Event o{ -- || Reservation : "concerne"
    Status o{ -- || Reservation : "est"

    Document o| -- }o Event : "concerne"